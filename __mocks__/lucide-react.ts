import * as React from 'react';

// Manual mock for lucide-react icons used in AddToiletForm
export const Camera = (props: any) => React.createElement('svg', { 'data-testid': 'mock-camera-icon', ...props });
export const MapPin = (props: any) => React.createElement('svg', { 'data-testid': 'mock-map-pin-icon', ...props });
export const Save = (props: any) => React.createElement('svg', { 'data-testid': 'mock-save-icon', ...props });
export const LocateFixed = (props: any) => React.createElement('svg', { 'data-testid': 'mock-locate-fixed-icon', ...props });
export const AlertTriangle = (props: any) => React.createElement('svg', { 'data-testid': 'mock-alert-triangle-icon', ...props });
export const Loader2 = (props: any) => React.createElement('svg', { 'data-testid': 'mock-loader2-icon', ...props });

// Existing Check mock (if still used elsewhere)
export const Check = (props: any) => null;
